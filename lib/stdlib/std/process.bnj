use std.platform;
use libc as c;

pub func exit(status: i32) {
    c.exit(status);
}

pub func env_var(name: StringSlice) -> ?String {
    return platform.env_var(name);
}

pub func env_vars() -> Array[(String, String)] {
    return platform.env_vars();
}
