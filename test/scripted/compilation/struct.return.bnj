#! output: 5 8 12

struct Point {
	var x: i32;
	var y: i32;
	var z: i32;
}

func create(x: i32, y: i32, z: i32) -> Point {
	var point: Point;
	point.x = x;
	point.y = y;
	point.z = z;
	return point;
}

func main() {
	var point = create(5, 8, 12);
	print(point.x);
	print(" ");
	print(point.y);
	print(" ");
	print(point.z);
}