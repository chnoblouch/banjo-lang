# test:output "true,false,false"

func eval(a: i32, b: bool) {
	if !(a == 3 || !b) {
		print("true");
	} else {
		print("false");
	}
}

func main() {
	eval(5, true);
	print(',');
	eval(3, false);
	print(',');
	eval(2, false);
}
