#! output: Point;Circle

struct Point {
	var x: i32;
	var y: i32;
}

struct Circle {
	var radius: i32;
	var color: u32;
}

func main() {
	print(type(Point).name);
	print(";");
	print(type(Circle).name);
}