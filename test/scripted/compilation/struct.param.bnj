#! output: 51

struct Vector {
	var x: i32;
	var y: i32;
	var z: i32;
}

func dot(a: Vector, b: Vector) -> i32 {
	return a.x * b.x + a.y * b.y + a.z * b.z;
}

func main() {
	var a: Vector;
	a.x = 5;
	a.y = 2;
	a.z = 3;
	
	var b: Vector;
	b.x = 8;
	b.y = 4;
	b.z = 1;
	
	print(dot(a, b));
}